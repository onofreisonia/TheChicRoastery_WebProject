services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chic-roastery
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true 
    command: npm run dev -- --host
